import Camera from '../Components/camera'

function Homepage() {
    return (
        <div>
            <p>Start the Camera to detect when a person is present and to record videocontent only when a person is present</p>
        <Camera />
    </div> )
}

export default Homepage